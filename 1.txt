bloquant par ça
Universel (RHEL7/8/9)
Yaml
Copy code
- name: Refresh package manager cache (RHEL7/8/9)
  ansible.builtin.package:
    update_cache: yes
ou bien (si tu préfères séparer) :
Yaml
Copy code
- name: Refresh cache (dnf)
  ansible.builtin.dnf:
    update_cache: yes
  when: ansible_pkg_mgr == "dnf"

- name: Refresh cache (yum)
  ansible.builtin.yum:
    update_cache: yes
  when: ansible_pkg_mgr == "yum"
🎯 Résultat final (le minimum